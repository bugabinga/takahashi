/* Aggregate header for build-system translate-c (b.addTranslateC).
   Exposes the miniaudio API to src/audio.zig. The implementation lives in
   vendor/miniaudio/miniaudio_impl.c; this header declares symbols only. */
#include "miniaudio.h"
