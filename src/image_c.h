/* Aggregate header for build-system translate-c (b.addTranslateC).
   Exposes the stb_image decode API to src/image.zig. The implementation lives
   in vendor/stb/stb_image_impl.c; this header declares symbols only. */
#include "stb_image.h"
